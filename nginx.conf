user nginx;
worker_processes auto;
# Force latest image build with nginx.conf fixes
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Logging
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    # Performance optimizations
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript
               application/json application/javascript application/xml+rss
               application/wasm;

    server {
        listen 8080;
        server_name _;
        root /usr/share/nginx/html;
        index index.html;

        # Security headers
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;

        # Framework files - serve directly without fallback to index.html
        # MUST be before the catch-all / location to take precedence
        location /_framework/ {
            # Allow direct file serving for all framework files
            try_files $uri =404;
        }

        # Blazor WebAssembly specific
        location / {
            try_files $uri $uri/ /index.html;
        }

        # Note: Blazor client uses fetch() with absolute URLs to call API
        # API URL is configured in appsettings.json and passed at build time
        # No need for proxy_pass here

        # HTML files - never cache, always revalidate
        location ~* \.html?$ {
            expires -1;
            add_header Cache-Control "no-cache, no-store, must-revalidate, public";
            add_header Pragma "no-cache";
        }

        # JSON configuration files - never cache
        location ~* \.json$ {
            expires -1;
            add_header Cache-Control "no-cache, no-store, must-revalidate, public";
            add_header Pragma "no-cache";
        }

        # Blazor boot file - MUST be first (highest priority, exact match)
        # Disable ETag to prevent 304 Not Modified (forces always 200)
        location = /_framework/blazor.boot.json {
            expires -1;
            etag off;
            add_header Cache-Control "no-cache, no-store, must-revalidate, public";
            add_header Pragma "no-cache";
        }

        # Framework JS files - NEVER cache, always revalidate
        # Must be before general WASM/framework files to take precedence
        location ~* ^/_framework/.*\.js$ {
            expires -1;
            add_header Cache-Control "no-cache, no-store, must-revalidate, public";
            add_header Pragma "no-cache";
        }

        # WASM files - NEVER cache, always revalidate (same as JS framework files)
        # must-revalidate allows 304 Not Modified responses which cause stale code to load
        location ~* \.wasm$ {
            types {
                application/wasm wasm;
            }
            expires -1;
            add_header Cache-Control "no-cache, no-store, must-revalidate, public";
            add_header Pragma "no-cache";
        }

        # Other framework files (CSS, JSON, etc.) - cache but allow revalidation
        location ~* ^/_framework/ {
            expires 1d;
            add_header Cache-Control "public, must-revalidate";
        }

        # Cache static assets (css, js, images, fonts)
        # Does NOT match _framework/* because that's handled above
        location ~* \.(css|jpg|jpeg|png|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }

        # Static JS files (NOT in _framework/ - those are handled above)
        location ~* \.js$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }

        # Health check endpoint
        location /health {
            access_log off;
            return 200 "healthy\n";
            add_header Content-Type text/plain;
        }
    }
}
